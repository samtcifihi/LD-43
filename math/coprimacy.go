package math

import (
"math/big"
)

// Tests if two numbers are coprime to eachother
func Coprime(a, b int) bool {
}

// Tests if any of the numbers in an array are coprime to eachother.
func Coprime(a []int) {
// Use "Range" to know when to stop the nested for loops
}
